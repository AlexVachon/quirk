*.log
*.ll
.vscode/settings.json

# Quirk venv / package-manager artifacts (project-local installs, lockfile
# generated by `quirk pkg install`, and test fixtures written by tests/sys_test.qk).
# NOTE: `quirk-compiler/packages/` is the *stdlib* (renamed from libs/ in 1.0.8) —
# it stays tracked. Third-party packages installed by `quirk pkg install` go under
# the project's own ./packages/ inside whichever Quirk project is using them.
quirk-compiler/.venv/
quirk-compiler/quirk.lock
quirk-compiler/test_output.txt

# Build outputs
quirk-compiler/bin/quirk
quirk-compiler/bin/runtime.so
quirk-compiler/obj

# Editor / extension
quirk-vscode/node_modules
quirk-vscode/out